DejaDup Launcher GNOME Shell extension
--------------------------------------

When the disk used by DejaDup for the backups is plugged, a button
is added to the run notification which allow the user to immediately
start a backup.
